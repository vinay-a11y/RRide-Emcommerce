MONGO_URL=mongodb://localhost:27017
DB_NAME=test_database
CORS_ORIGINS=*
JWT_SECRET_KEY=784dbcb9ddaec43b688925c8fe24011d68cccae0138b2deef70dca349765ecad
RAZORPAY_KEY_ID=rzp_test_m5j63eVkzEwHxa
RAZORPAY_KEY_SECRET=45ZCZTvLVb2GjiKo2n9meKTB
DATABASE_URL=mysql://root:143@Vinay@localhost:3306/bike_shop

# -- Bike Shop E-Commerce Database Schema
# -- MySQL 8.0+

# -- Create Database
# CREATE DATABASE IF NOT EXISTS bike_shop;
# USE bike_shop;

# -- ============ USERS TABLE ============
# CREATE TABLE users (
#     id VARCHAR(36) PRIMARY KEY,
#     mobile VARCHAR(20) NOT NULL UNIQUE,
#     password_hash VARCHAR(255) NOT NULL,
#     name VARCHAR(100),
#     email VARCHAR(100),
#     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#     updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
#     INDEX idx_mobile (mobile)
# );

# -- ============ BIKES TABLE ============
# -- Stores all bike models available for compatibility matching
# CREATE TABLE bikes (
#     id VARCHAR(36) PRIMARY KEY,
#     brand VARCHAR(100) NOT NULL,
#     model VARCHAR(100) NOT NULL,
#     variant VARCHAR(100) NOT NULL,
#     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#     UNIQUE KEY unique_bike (brand, model, variant),
#     INDEX idx_brand (brand),
#     INDEX idx_model (model)
# );

# -- ============ PRODUCT BRANDS TABLE ============
# -- Stores brands/manufacturers of products
# CREATE TABLE product_brands (
#     id VARCHAR(36) PRIMARY KEY,
#     name VARCHAR(100) NOT NULL UNIQUE,
#     logo VARCHAR(255),
#     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#     INDEX idx_name (name)
# );

# -- ============ SPARE CATEGORIES TABLE ============
# -- Categories: Performance, Safety Gear, Pro Spares, Accessories, etc.
# CREATE TABLE spare_categories (
#     id VARCHAR(36) PRIMARY KEY,
#     name VARCHAR(100) NOT NULL,
#     slug VARCHAR(100) NOT NULL UNIQUE,
#     description TEXT,
#     image VARCHAR(255),
#     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#     INDEX idx_slug (slug)
# );

# -- ============ PRODUCTS TABLE ============
# CREATE TABLE products (
#     id VARCHAR(36) PRIMARY KEY,
#     name VARCHAR(255) NOT NULL,
#     brand_id VARCHAR(36),
#     spare_category_id VARCHAR(36),
#     price DECIMAL(10, 2) NOT NULL,
#     original_price DECIMAL(10, 2),
#     description LONGTEXT,
#     images JSON,
#     specifications JSON,
#     stock INT DEFAULT 0,
#     rating DECIMAL(3, 2) DEFAULT 4.5,
#     reviews_count INT DEFAULT 0,
#     is_best_seller BOOLEAN DEFAULT FALSE,
#     is_new_arrival BOOLEAN DEFAULT FALSE,
#     installation_difficulty VARCHAR(50) DEFAULT 'Medium',
#     warranty VARCHAR(100) DEFAULT '1 Year',
#     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#     updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
#     FOREIGN KEY (brand_id) REFERENCES product_brands(id) ON DELETE SET NULL,
#     FOREIGN KEY (spare_category_id) REFERENCES spare_categories(id) ON DELETE SET NULL,
#     INDEX idx_brand (brand_id),
#     INDEX idx_category (spare_category_id),
#     INDEX idx_price (price),
#     INDEX idx_best_seller (is_best_seller),
#     FULLTEXT INDEX ft_name (name)
# );

# -- ============ PRODUCT BIKE COMPATIBILITY TABLE ============
# -- Links products to compatible bikes (Many-to-Many)
# CREATE TABLE product_bike_compatibility (
#     product_id VARCHAR(36) NOT NULL,
#     bike_id VARCHAR(36) NOT NULL,
#     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#     PRIMARY KEY (product_id, bike_id),
#     FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
#     FOREIGN KEY (bike_id) REFERENCES bikes(id) ON DELETE CASCADE,
#     INDEX idx_bike (bike_id)
# );

# -- ============ CARTS TABLE ============
# CREATE TABLE carts (
#     id VARCHAR(36) PRIMARY KEY,
#     user_id VARCHAR(36) NOT NULL UNIQUE,
#     items JSON NOT NULL DEFAULT '[]',
#     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#     updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
#     FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
#     INDEX idx_user (user_id)
# );

# -- ============ ORDERS TABLE ============
# CREATE TABLE orders (
#     id VARCHAR(36) PRIMARY KEY,
#     user_id VARCHAR(36) NOT NULL,
#     items JSON NOT NULL,
#     total_amount DECIMAL(10, 2) NOT NULL,
#     shipping_address JSON,
#     payment_method VARCHAR(50),
#     payment_status VARCHAR(20) DEFAULT 'pending',
#     order_status VARCHAR(20) DEFAULT 'placed',
#     razorpay_order_id VARCHAR(100),
#     razorpay_payment_id VARCHAR(100),
#     notes TEXT,
#     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#     updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
#     FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
#     INDEX idx_user (user_id),
#     INDEX idx_status (order_status),
#     INDEX idx_created (created_at)
# );

# -- ============ WISHLISTS TABLE ============
# CREATE TABLE wishlists (
#     id VARCHAR(36) PRIMARY KEY,
#     user_id VARCHAR(36) NOT NULL,
#     product_id VARCHAR(36) NOT NULL,
#     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#     UNIQUE KEY unique_wishlist (user_id, product_id),
#     FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
#     FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
#     INDEX idx_user (user_id)
# );

# -- ============ REVIEWS TABLE ============
# CREATE TABLE reviews (
#     id VARCHAR(36) PRIMARY KEY,
#     product_id VARCHAR(36) NOT NULL,
#     user_id VARCHAR(36) NOT NULL,
#     rating INT NOT NULL CHECK (rating >= 1 AND rating <= 5),
#     comment TEXT,
#     helpful_count INT DEFAULT 0,
#     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
#     FOREIGN KEY (product_id) REFERENCES products(id) ON DELETE CASCADE,
#     FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
#     INDEX idx_product (product_id),
#     INDEX idx_user (user_id)
# );

# -- ============ SAMPLE DATA ============

# -- Insert Bikes
# INSERT INTO bikes (id, brand, model, variant) VALUES
# ('bike-ktm-1', 'KTM', 'Duke 390', '2024'),
# ('bike-ktm-2', 'KTM', 'Duke 390', '2023'),
# ('bike-ktm-3', 'KTM', 'Duke 250', '2024'),
# ('bike-ktm-4', 'KTM', 'RC 390', '2024'),
# ('bike-bmw-1', 'BMW', 'G 310 R', '2024'),
# ('bike-bmw-2', 'BMW', 'G 310 GS', '2024'),
# ('bike-royal-1', 'Royal Enfield', 'Classic 350', '2024'),
# ('bike-royal-2', 'Royal Enfield', 'Meteor 350', '2024'),
# ('bike-yamaha-1', 'Yamaha', 'MT-15', '2024'),
# ('bike-yamaha-2', 'Yamaha', 'R15 V4', '2024');

# -- Insert Product Brands
# INSERT INTO product_brands (id, name, logo) VALUES
# ('brand-1', 'Akrapovic', 'https://via.placeholder.com/100?text=Akrapovic'),
# ('brand-2', 'Motul', 'https://via.placeholder.com/100?text=Motul'),
# ('brand-3', 'Brembo', 'https://via.placeholder.com/100?text=Brembo'),
# ('brand-4', 'Michelin', 'https://via.placeholder.com/100?text=Michelin'),
# ('brand-5', 'Bosch', 'https://via.placeholder.com/100?text=Bosch');

# -- Insert Spare Categories
# INSERT INTO spare_categories (id, name, slug, description) VALUES
# ('cat-1', 'Performance', 'performance', 'High performance upgrades for maximum power'),
# ('cat-2', 'Safety Gear', 'safety_gear', 'Protective equipment for riders'),
# ('cat-3', 'Pro Spares', 'pro_spares', 'Professional replacement parts'),
# ('cat-4', 'Accessories', 'accessories', 'Add-ons and customization parts');

# -- Insert Sample Products
# INSERT INTO products (id, name, brand_id, spare_category_id, price, original_price, description, images, stock, rating, reviews_count, is_best_seller, is_new_arrival) VALUES
# ('prod-1', 'Akrapovic Exhaust System', 'brand-1', 'cat-1', 18999, 22999, 'Premium exhaust system with enhanced performance', '["https://via.placeholder.com/400?text=Akrapovic"]', 15, 4.8, 42, TRUE, FALSE),
# ('prod-2', 'Motul Engine Oil 5W-40', 'brand-2', 'cat-3', 2499, 3299, 'Synthetic engine oil for superior protection', '["https://via.placeholder.com/400?text=Motul"]', 50, 4.7, 128, TRUE, FALSE),
# ('prod-3', 'Brembo Brake Pads', 'brand-3', 'cat-3', 5999, 7499, 'High-performance brake pads for better stopping power', '["https://via.placeholder.com/400?text=Brembo"]', 30, 4.9, 87, FALSE, TRUE),
# ('prod-4', 'Michelin Pilot Road 5 Tire', 'brand-4', 'cat-3', 12999, 15999, 'Premium tire for ultimate grip and safety', '["https://via.placeholder.com/400?text=Michelin"]', 20, 4.6, 95, FALSE, FALSE),
# ('prod-5', 'Riding Jacket Textile', 'brand-5', 'cat-2', 8999, 11999, 'Premium textile riding jacket with armor protection', '["https://via.placeholder.com/400?text=Jacket"]', 25, 4.5, 34, FALSE, TRUE);

# -- Insert Product-Bike Compatibility
# INSERT INTO product_bike_compatibility (product_id, bike_id) VALUES
# ('prod-1', 'bike-ktm-1'),
# ('prod-1', 'bike-ktm-2'),
# ('prod-1', 'bike-ktm-3'),
# ('prod-2', 'bike-ktm-1'),
# ('prod-2', 'bike-ktm-2'),
# ('prod-2', 'bike-bmw-1'),
# ('prod-3', 'bike-ktm-1'),
# ('prod-3', 'bike-royal-1'),
# ('prod-4', 'bike-ktm-1'),
# ('prod-4', 'bike-yamaha-1'),
# ('prod-5', 'bike-ktm-1'),
# ('prod-5', 'bike-bmw-1'),
# ('prod-5', 'bike-royal-1');

# -- ============ STORED PROCEDURES ============

# DELIMITER $$

# -- Get compatible products for a bike
# CREATE PROCEDURE GetProductsForBike (
#     IN p_brand VARCHAR(100),
#     IN p_model VARCHAR(100),
#     IN p_variant VARCHAR(100)
# )
# BEGIN
#     SELECT DISTINCT p.*
#     FROM products p
#     JOIN product_bike_compatibility pbc ON p.id = pbc.product_id
#     JOIN bikes b ON pbc.bike_id = b.id
#     WHERE b.brand = p_brand
#     AND b.model = p_model
#     AND b.variant = p_variant
#     ORDER BY p.is_best_seller DESC, p.rating DESC;
# END$$

# -- Get product stats
# CREATE PROCEDURE GetProductStats ()
# BEGIN
#     SELECT
#         COUNT(DISTINCT id) as total_products,
#         COUNT(DISTINCT spare_category_id) as total_categories,
#         COUNT(DISTINCT brand_id) as total_brands,
#         SUM(stock) as total_stock,
#         AVG(rating) as avg_rating
#     FROM products;
# END$$

# DELIMITER ;

# -- ============ VIEWS ============

# -- Popular products view
# CREATE VIEW popular_products AS
# SELECT
#     id,
#     name,
#     price,
#     rating,
#     reviews_count,
#     is_best_seller,
#     is_new_arrival  
# FROM products
# ORDER BY (rating * reviews_count) DESC
# LIMIT 100;

# -- Product inventory view
# CREATE VIEW product_inventory AS
# SELECT
#     id,
#     name,
#     stock,
#     price,
#     CASE
#         WHEN stock = 0 THEN 'Out of Stock'
#         WHEN stock < 5 THEN 'Low Stock'
#         ELSE 'In Stock'
#     END as stock_status
# FROM products;
